html
    head
        meta(charset="utf-8")
        style
            include ../style/main.css
    body
        div(class="wrapper")
            h1(class="greeting") Kedes Moderátor!
            h2(class="sub-greeting") Az Évgyűrű Alapítvány honlapján a következő kurzusnál aktivitás történt:
            p(class="course-title") #{courseTitle}
            p(class="parent-name") #{parentName}
            p(class=isApplied ? "activity-status green-status": "activity-status red-status") #{isApplied ? 'Új foglalás' : 'Foglalás törölve'}
            div(class="children")
                div(class="children-title") Jelentkeztetett gyermek(ek):
                ul(class="children-list")
                  each child in children
                      li(class="list-element")=child.name
    footer
        div(class="footer-wrapper")
            p © 2022 Évgyűrű Alapítvány
